## 1. Outlook on the web URL生成

- [x] 1.1 `MailDraft` のTo・CC・BCCをこの順でToへ統合した Outlook on the web のcompose deep linkを生成し、`cc` / `bcc` パラメータを付与せず、複数宛先・件名・本文を `%20` 形式でURLエンコードする
- [x] 1.2 URL生成ユニットテストを、ベースURL、複数のTo/CC/BCCのTo統合、`cc` / `bcc` パラメータの不使用、空パラメータ、日本語、改行、スペース、および `+` を使用しないことまで網羅するよう更新する

## 2. メール作成UI

- [x] 2.1 確認ボタンのユーザー操作内で、非同期API処理より先に Outlook on the web を新しいタブで開き、元タブへの参照を渡さないようにする
- [x] 2.2 Outlook on the web を開く操作後も、既存のメール作成済み記録と送信済み自己申告フローを維持する
- [x] 2.3 確認画面、作成ボタン、長いURLの警告、コピー用フォールバック、送信済み自己申告の文言を Outlook on the web 前提へ更新し、登録上のCC/BCCもToとして全員に表示されることを明示する
- [x] 2.4 会社のM365アカウントへの事前サインインと、Outlook作成画面でのFrom確認を利用者へ明示する

## 3. ドキュメント

- [x] 3.1 READMEの機能説明と制約を、`mailto:`／デスクトップ版Outlookから Outlook on the web のcompose deep link方式へ更新し、全宛先がToとして表示される制約を明記する
- [x] 3.2 アーキテクチャ図、メール作成シーケンス、コンポーネント責務、To統合を含む設計判断を新しい方式へ更新する
- [x] 3.3 セットアップガイドの前提条件とトラブルシューティングに、M365サインイン、複数アカウント、To統合、タブブロック、入力値欠落時のコピー手順を反映する

## 4. 検証

- [x] 4.1 フロントエンドのユニットテストとプロダクションビルドを実行し、回帰がないことを確認する
- [x] 4.2 サインイン済みの会社M365アカウントで、登録上のTo・CC・BCCがすべてToとして入力され、件名・日本語本文・改行が入力済みの新規メール作成画面を新しいタブで開けることを実機確認する
- [x] 4.3 元のlabormanタブが維持され、メール作成済み記録と送信済み自己申告を継続できることを実機確認する
- [x] 4.4 未サインイン、複数アカウント、タブブロック、長い本文を確認し、問題発生時にコピー用フォールバックで復旧できることを実機確認する
- [x] 4.5 `openspec validate use-outlook-web-compose --strict --no-interactive` を実行し、changeがstrict validationを通過することを確認する
